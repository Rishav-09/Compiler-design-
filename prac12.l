%{
	#include<stdio.h>
	int v = 0, c = 0;
%}
%%
[aeiouAEIOU] {v++;}
[a-zA-Z] {c++;}
\n ;
. ;
%%
int main() {
	printf("Enter the Input String: ");
	yylex();
	printf("Number of vowels are: %d\n", v);
	printf("Number of consonents are: %d\n", c);
	return 0;
}
int yywrap() {return 1;}